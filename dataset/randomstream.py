import numpy as np
import logging
from collections import Counter

def create_random_stream(n,m):
    assert m >= w
    # np.random.seed(42)
    stream = np.random.randint(1,n+1,size=m)
    return stream

# def 